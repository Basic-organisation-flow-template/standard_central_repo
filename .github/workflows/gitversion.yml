name: GitVersion Semantic Versioning

on:
  workflow_call:  # Allows this workflow to be called from other repositories

jobs:
  gitversion:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0.13.2
        with:
          versionSpec: 6.0.0

      - name: Show last commit
        run: git log -1



      - name: Debug working directory
        run: |
          pwd
          ls -la
      - name: Show branch name 
        run: git rev-parse --abbrev-ref HEAD
      - name: Show GitVersion config
        run: cat .gitversion.yml
      - name: Show GitVersion effective config
        run: |
          dotnet tool install --global GitVersion.Tool --version 6.0.0
          export PATH="$PATH:/home/runner/.dotnet/tools"
          dotnet-gitversion /showconfig



      - name: Run GitVersion
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0.13.2
        with:
          versionSpec: 6.0.0

      - name: Show GitVersion outputs
        run: |
          echo "FullSemVer: ${{ steps.gitversion.outputs.fullSemVer }}"
          echo "Major: ${{ steps.gitversion.outputs.major }}"
          echo "Minor: ${{ steps.gitversion.outputs.minor }}"
          echo "Patch: ${{ steps.gitversion.outputs.patch }}"

# the general default setting of action -> workflow of organisation was modified (use PAT instead)
      - name: Create and push git tag
        run: |
          GIT_AUTHOR_NAME="$(git log -1 --pretty=format:'%an')"
          GIT_AUTHOR_EMAIL="$(git log -1 --pretty=format:'%ae')"
          git config user.name "$GIT_AUTHOR_NAME"
          git config user.email "$GIT_AUTHOR_EMAIL"
          git tag "v${{ steps.gitversion.outputs.fullSemVer }}"
          git push origin "v${{ steps.gitversion.outputs.fullSemVer }}"


###
          # VERSION="${{ steps.gitversion.outputs.majorMinorPatch }}"
          # LABEL="${{ steps.gitversion.outputs.preReleaseLabel }}"
          # if [ -n "$LABEL" ]; then
          #   TAG="$VERSION-$LABEL"
          # else
          #   TAG="$VERSION"
          # fi
          # git tag $TAG
          # git push origin $TAG

